import React from 'react';
import ReactDOM from 'react-dom';
import Home from './view/h5/home'

ReactDOM.render(
  <React.StrictMode>
    <Home></Home>
  </React.StrictMode>,
  document.getElementById('root')
);
